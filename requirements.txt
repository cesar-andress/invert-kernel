# Pinned from pyproject.toml [project.dependencies] (INVERT Kernel v1.0.0).
# Install editable package: pip install -e ".[dev]"
typer>=0.9.0
pyyaml>=6.0
matplotlib>=3.7.0
openai>=1.0.0
anthropic>=0.25.0
google-generativeai>=0.5.0

# Development / verification (optional):
# pytest>=7.0.0
